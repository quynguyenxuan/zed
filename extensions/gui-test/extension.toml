id = "gui-test"
name = "GUI Test"
description = "A test extension that renders a simple GUI panel."
version = "0.1.2"
schema_version = 1
authors = ["Zed Dev <dev@zed.dev>"]
repository = "https://github.com/zed-industries/zed"

[lib]
kind = "Rust"
version = "0.9.0"

[panel_ui]
min_zed_api = "0.9.0"

[[capabilities]]
kind = "process:exec"
command = "git"
args = ["-C", "**"]
